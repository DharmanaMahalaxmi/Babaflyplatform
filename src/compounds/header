import { 
  CheckCircle2, 
  ShieldCheck, 
  Terminal, 
  Download, 
  ShoppingBag, 
  UserCheck, 
  LogOut, 
  KeyRound, 
  UserPlus, 
  Shield, 
  Sparkles,
  User as UserIcon,
  Crown
} from 'lucide-react';
import { User } from '../types';

export type AppTab = 'user_portal' | 'admin_portal' | 'explorer' | 'tests' | 'code' | 'submission';

interface HeaderProps {
  activeTab: AppTab;
  setActiveTab: (tab: AppTab) => void;
  currentUser: User | null;
  onLoginDemo: (role: 'admin' | 'user') => void;
  onLogout: () => void;
  onDownloadZip: () => void;
  onOpenNewUserChecker: () => void;
}

export function Header({
  activeTab,
  setActiveTab,
  currentUser,
  onLoginDemo,
  onLogout,
  onDownloadZip,
  onOpenNewUserChecker,
}: HeaderProps) {
  const isAdmin = currentUser?.role === 'admin';

  return (
    <header className="bg-slate-900 border-b border-slate-800 sticky top-0 z-40 text-slate-100 shadow-md">
      {/* Top Brand & Status Strip */}
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-3 flex flex-wrap items-center justify-between gap-4">
        {/* Logo & Title */}
        <div className="flex items-center gap-3">
          <div className="w-10 h-10 rounded-xl bg-amber-500/10 border border-amber-500/30 flex items-center justify-center text-amber-400 font-bold text-lg shadow-sm">
            BF
          </div>
          <div>
            <div className="flex items-center gap-2">
              <h1 className="text-lg font-semibold tracking-tight text-white">BabaFly Platform</h1>
              <span className="text-xs px-2 py-0.5 rounded-full bg-emerald-500/15 border border-emerald-500/30 text-emerald-400 font-mono font-medium flex items-center gap-1">
                <span className="w-1.5 h-1.5 rounded-full bg-emerald-400 animate-pulse"></span>
                FastAPI v1.0
              </span>
            </div>
            <p className="text-xs text-slate-400">
              High Jewelry Backend • FastAPI • PostgreSQL • JWT Auth • Pytest
            </p>
          </div>
        </div>

        {/* Auth Links & Action Controls */}
        <div className="flex items-center flex-wrap gap-2.5 text-xs">
          {/* Check New User Button - High Prominence */}
          <button
            onClick={onOpenNewUserChecker}
            className="flex items-center gap-1.5 bg-blue-600 hover:bg-blue-500 text-white font-semibold px-3 py-1.5 rounded-lg shadow-sm transition-all cursor-pointer"
            title="Automated 7-step test or custom registration of a new user"
          >
            <UserPlus className="w-3.5 h-3.5" />
            <span>Check Once New User</span>
          </button>

          {/* User Session or Quick Login Switches */}
          {currentUser ? (
            <div className="flex items-center gap-2 bg-slate-800/90 border border-slate-700 px-3 py-1.5 rounded-lg">
              <UserCheck className="w-3.5 h-3.5 text-emerald-400" />
              <div className="flex items-center gap-1.5">
                <span className="text-slate-200 font-medium max-w-[130px] truncate">{currentUser.name}</span>
                <span className={`text-[10px] uppercase tracking-wider font-bold px-1.5 py-0.5 rounded ${
                  isAdmin 
                    ? 'bg-amber-500/20 text-amber-300 border border-amber-500/40 flex items-center gap-1' 
                    : 'bg-blue-500/20 text-blue-300 border border-blue-500/30 flex items-center gap-1'
                }`}>
                  {isAdmin ? <Crown className="w-2.5 h-2.5" /> : <UserIcon className="w-2.5 h-2.5" />}
                  {currentUser.role}
                </span>
              </div>

              {/* Shortcut to relevant portal */}
              {isAdmin ? (
                <button
                  onClick={() => setActiveTab('admin_portal')}
                  className="text-[11px] text-amber-400 hover:text-amber-300 underline ml-1 cursor-pointer font-medium"
                >
                  Admin Link
                </button>
              ) : (
                <button
                  onClick={() => setActiveTab('user_portal')}
                  className="text-[11px] text-blue-400 hover:text-blue-300 underline ml-1 cursor-pointer font-medium"
                >
                  User Link
                </button>
              )}

              <button
                onClick={onLogout}
                title="Log out session"
                className="text-slate-400 hover:text-rose-400 transition-colors ml-1 p-0.5 cursor-pointer"
              >
                <LogOut className="w-3.5 h-3.5" />
              </button>
            </div>
          ) : (
            <div className="flex items-center gap-1.5 bg-slate-800/80 border border-slate-700 p-1 rounded-lg">
              <span className="text-slate-400 px-1.5 flex items-center gap-1">
                <KeyRound className="w-3.5 h-3.5 text-slate-400" /> Switch Auth:
              </span>
              <button
                onClick={() => onLoginDemo('admin')}
                className="px-2.5 py-1 rounded bg-amber-500/20 hover:bg-amber-500/30 text-amber-300 font-medium border border-amber-500/30 transition-all cursor-pointer"
              >
                Admin JWT
              </button>
              <button
                onClick={() => onLoginDemo('user')}
                className="px-2.5 py-1 rounded bg-blue-500/20 hover:bg-blue-500/30 text-blue-300 font-medium border border-blue-500/30 transition-all cursor-pointer"
              >
                User JWT
              </button>
            </div>
          )}

          {/* Download Zip */}
          <button
            onClick={onDownloadZip}
            className="flex items-center gap-1.5 bg-gradient-to-r from-amber-600 to-amber-500 hover:from-amber-500 hover:to-amber-400 text-white font-medium px-3.5 py-1.5 rounded-lg shadow-sm transition-all cursor-pointer"
          >
            <Download className="w-3.5 h-3.5" />
            <span className="hidden sm:inline">Download Python Repo (.ZIP)</span>
            <span className="sm:hidden">.ZIP</span>
          </button>
        </div>
      </div>

      {/* Main Tab Navigation - Separated Admin and User Links */}
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <nav className="flex items-center space-x-1 border-t border-slate-800/80 pt-1 overflow-x-auto no-scrollbar">
          {/* 1. SEPARATE USER LINK */}
          <button
            onClick={() => setActiveTab('user_portal')}
            className={`px-3.5 py-2.5 text-xs sm:text-sm font-semibold border-b-2 transition-all flex items-center gap-2 cursor-pointer shrink-0 ${
              activeTab === 'user_portal'
                ? 'border-blue-400 text-blue-400 bg-blue-500/10'
                : 'border-transparent text-slate-400 hover:text-slate-200 hover:border-slate-700'
            }`}
          >
            <ShoppingBag className="w-4 h-4 text-blue-400" />
            <span>User Portal</span>
            <span className="text-[10px] uppercase font-mono px-1.5 py-0.2 rounded bg-blue-500/20 text-blue-300 border border-blue-500/30">
              Customer
            </span>
          </button>

          {/* 2. SEPARATE ADMIN LINK */}
          <button
            onClick={() => setActiveTab('admin_portal')}
            className={`px-3.5 py-2.5 text-xs sm:text-sm font-semibold border-b-2 transition-all flex items-center gap-2 cursor-pointer shrink-0 ${
              activeTab === 'admin_portal'
                ? 'border-amber-400 text-amber-400 bg-amber-500/10'
                : 'border-transparent text-slate-400 hover:text-amber-300 hover:border-amber-500/40'
            }`}
          >
            <Shield className="w-4 h-4 text-amber-400" />
            <span>Admin Portal</span>
            <span className="text-[10px] uppercase font-mono px-1.5 py-0.2 rounded bg-amber-500/20 text-amber-300 border border-amber-500/30">
              CRUD & RBAC
            </span>
          </button>

          {/* Visual Divider between Portals and Tooling */}
          <div className="h-5 w-px bg-slate-800 mx-1 shrink-0" />

          {/* 3. API Explorer */}
          <button
            onClick={() => setActiveTab('explorer')}
            className={`px-3 py-2.5 text-xs sm:text-sm font-medium border-b-2 transition-all flex items-center gap-2 cursor-pointer shrink-0 ${
              activeTab === 'explorer'
                ? 'border-amber-400 text-amber-400 bg-amber-500/5'
                : 'border-transparent text-slate-400 hover:text-slate-200 hover:border-slate-700'
            }`}
          >
            <Terminal className="w-4 h-4" />
            <span>API Explorer</span>
          </button>

          {/* 4. Pytest Suite */}
          <button
            onClick={() => setActiveTab('tests')}
            className={`px-3 py-2.5 text-xs sm:text-sm font-medium border-b-2 transition-all flex items-center gap-2 cursor-pointer shrink-0 ${
              activeTab === 'tests'
                ? 'border-amber-400 text-amber-400 bg-amber-500/5'
                : 'border-transparent text-slate-400 hover:text-slate-200 hover:border-slate-700'
            }`}
          >
            <CheckCircle2 className="w-4 h-4 text-emerald-400" />
            <span>Pytest Suite (5 Tests)</span>
          </button>

          {/* 5. Python Codebase */}
          <button
            onClick={() => setActiveTab('code')}
            className={`px-3 py-2.5 text-xs sm:text-sm font-medium border-b-2 transition-all flex items-center gap-2 cursor-pointer shrink-0 ${
              activeTab === 'code'
                ? 'border-amber-400 text-amber-400 bg-amber-500/5'
                : 'border-transparent text-slate-400 hover:text-slate-200 hover:border-slate-700'
            }`}
          >
            <ShieldCheck className="w-4 h-4" />
            <span>Python Code</span>
          </button>

          {/* 6. Submission Guide */}
          <button
            onClick={() => setActiveTab('submission')}
            className={`px-3 py-2.5 text-xs sm:text-sm font-medium border-b-2 transition-all flex items-center gap-2 cursor-pointer shrink-0 ${
              activeTab === 'submission'
                ? 'border-amber-400 text-amber-400 bg-amber-500/5'
                : 'border-transparent text-slate-400 hover:text-slate-200 hover:border-slate-700'
            }`}
          >
            <CheckCircle2 className="w-4 h-4 text-amber-400" />
            <span>Submission & Video</span>
          </button>
        </nav>
      </div>
    </header>
  );
}
