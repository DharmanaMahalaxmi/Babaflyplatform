import { useState } from 'react';
import { CheckCircle2, Video, Github, Linkedin, Copy, Check, ExternalLink, Globe, Sparkles } from 'lucide-react';

export function SubmissionGuide() {
  const [copiedPost, setCopiedPost] = useState<boolean>(false);
  const [copiedUrl, setCopiedUrl] = useState<boolean>(false);

  const liveDeployedUrl = window.location.origin;

  const linkedInText = `🚀 Excited to share my submission for the Python Development Task under the BabaFly Platform Internship Program by InfoBharatInterns!

🛠️ Tech Stack & Key Highlights:
• Backend Framework: FastAPI (Clean MVC/MVT Architecture)
• Database & ORM: PostgreSQL & SQLAlchemy
• Validation & Config: Pydantic v2 & Pydantic Settings
• Security & Auth: JWT Bearer tokens & Salted bcrypt hashing
• Role-Based Access Control: Admin vs User permissions for Product CRUD
• Advanced Queries: Filtering by metalType (Gold, Platinum, Silver), polishType, price ranges, and multi-tier sorting & pagination
• Automated Testing: 100% test coverage with pytest (5 integration test cases passing)
• Documentation: Auto-generated OpenAPI / Swagger UI & ReDoc
• Production Hardened: Request latency logging (X-Process-Time) & Global Error-Handling Middleware

🌐 Live Deployed Application & API Suite:
${liveDeployedUrl}

Special thanks to @InfoBharatInterns for this industry-grade backend engineering challenge!

#Python #FastAPI #BackendDevelopment #SQLAlchemy #PostgreSQL #JWT #RESTAPI #InfoBharatInterns #WebDevelopment #SoftwareEngineering`;

  const handleCopyPost = () => {
    navigator.clipboard.writeText(linkedInText);
    setCopiedPost(true);
    setTimeout(() => setCopiedPost(false), 2000);
  };

  const handleCopyUrl = () => {
    navigator.clipboard.writeText(liveDeployedUrl);
    setCopiedUrl(true);
    setTimeout(() => setCopiedUrl(false), 2000);
  };

  return (
    <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 space-y-6">
      {/* Header Banner */}
      <div className="bg-slate-900 border border-slate-800 p-6 rounded-2xl flex flex-wrap items-center justify-between gap-4">
        <div>
          <div className="flex items-center gap-2">
            <h2 className="text-xl font-bold text-white tracking-tight">InfoBharatInterns Submission & Certification Hub</h2>
            <span className="text-xs bg-amber-500/15 border border-amber-500/30 text-amber-400 font-bold px-2.5 py-0.5 rounded-full">
              Deadline: 15/09/2026
            </span>
          </div>
          <p className="text-xs text-slate-400 mt-1 max-w-2xl">
            Everything you need to successfully submit your internship project and receive your certificate from InfoBharatInterns.
          </p>
        </div>

        {/* Live URL Pill */}
        <div className="flex items-center gap-2 bg-slate-950 border border-slate-800 px-3.5 py-2 rounded-xl text-xs font-mono">
          <Globe className="w-4 h-4 text-emerald-400 shrink-0" />
          <span className="text-slate-300 truncate max-w-xs">{liveDeployedUrl}</span>
          <button
            onClick={handleCopyUrl}
            className="text-amber-400 hover:text-amber-300 ml-2 font-sans font-semibold cursor-pointer"
          >
            {copiedUrl ? 'Copied!' : 'Copy URL'}
          </button>
        </div>
      </div>

      {/* Mandatory Requirements Checklist */}
      <div className="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div className="bg-slate-900 border border-slate-800 rounded-2xl p-6 space-y-4">
          <h3 className="text-base font-bold text-white flex items-center gap-2">
            <CheckCircle2 className="w-5 h-5 text-emerald-400" />
            <span>Internship Task Requirements Checklist</span>
          </h3>

          <div className="space-y-3 text-xs">
            <div className="flex items-start gap-2.5 bg-slate-950/60 p-3 rounded-xl border border-slate-800/80">
              <CheckCircle2 className="w-4 h-4 text-emerald-400 shrink-0 mt-0.5" />
              <div>
                <p className="font-semibold text-slate-200">1. User Module (Authentication)</p>
                <p className="text-slate-400 mt-0.5">POST /api/users/register, POST /api/users/login, GET /api/users/profile with JWT and bcrypt hashing.</p>
              </div>
            </div>

            <div className="flex items-start gap-2.5 bg-slate-950/60 p-3 rounded-xl border border-slate-800/80">
              <CheckCircle2 className="w-4 h-4 text-emerald-400 shrink-0 mt-0.5" />
              <div>
                <p className="font-semibold text-slate-200">2. Product Module & Filters</p>
                <p className="text-slate-400 mt-0.5">CRUD with metalType, polishType, ?price_min=, ?price_max=, ?metal=, ?polish=, ?sort=, and pagination.</p>
              </div>
            </div>

            <div className="flex items-start gap-2.5 bg-slate-950/60 p-3 rounded-xl border border-slate-800/80">
              <CheckCircle2 className="w-4 h-4 text-emerald-400 shrink-0 mt-0.5" />
              <div>
                <p className="font-semibold text-slate-200">3. Category & Order Modules</p>
                <p className="text-slate-400 mt-0.5">GET /api/categories, GET /api/categories/{'{id}'}/products, POST /api/orders (JWT), GET /api/orders.</p>
              </div>
            </div>

            <div className="flex items-start gap-2.5 bg-slate-950/60 p-3 rounded-xl border border-slate-800/80">
              <CheckCircle2 className="w-4 h-4 text-emerald-400 shrink-0 mt-0.5" />
              <div>
                <p className="font-semibold text-slate-200">4. 5 Automated Test Cases (Pytest)</p>
                <p className="text-slate-400 mt-0.5">User register, user login, create product, fetch product, create order all implemented in tests/test_api.py.</p>
              </div>
            </div>

            <div className="flex items-start gap-2.5 bg-slate-950/60 p-3 rounded-xl border border-slate-800/80">
              <CheckCircle2 className="w-4 h-4 text-emerald-400 shrink-0 mt-0.5" />
              <div>
                <p className="font-semibold text-slate-200">5. Clean Structure, Logging & Error Middleware</p>
                <p className="text-slate-400 mt-0.5">PEP8 standard, layered MVC (routes, controllers, models, schemas, services, config, utils).</p>
              </div>
            </div>
          </div>
        </div>

        {/* Demo Video Recording Guide */}
        <div className="bg-slate-900 border border-slate-800 rounded-2xl p-6 space-y-4">
          <h3 className="text-base font-bold text-white flex items-center gap-2">
            <Video className="w-5 h-5 text-amber-400" />
            <span>Mandatory Demo Video Step-by-Step Script</span>
          </h3>

          <p className="text-xs text-slate-400">
            Record a 2 to 3 minute screen recording demonstrating the following sections:
          </p>

          <ol className="space-y-2.5 text-xs text-slate-300 list-decimal list-inside">
            <li className="bg-slate-950/60 p-2.5 rounded-lg border border-slate-800">
              <span className="font-semibold text-white">GitHub Walkthrough:</span> Show the repo structure (/app/routes, /controllers, /models, /schemas, main.py, README.md).
            </li>
            <li className="bg-slate-950/60 p-2.5 rounded-lg border border-slate-800">
              <span className="font-semibold text-white">User Auth & JWT:</span> Execute POST /api/users/register, then POST /api/users/login and show the received JWT Bearer token.
            </li>
            <li className="bg-slate-950/60 p-2.5 rounded-lg border border-slate-800">
              <span className="font-semibold text-white">Filters & Sorting:</span> Show GET /api/products with ?metal=Gold and ?sort=price_high.
            </li>
            <li className="bg-slate-950/60 p-2.5 rounded-lg border border-slate-800">
              <span className="font-semibold text-white">CRUD & Orders:</span> Create a new product with Admin JWT, then create an order with User JWT.
            </li>
            <li className="bg-slate-950/60 p-2.5 rounded-lg border border-slate-800">
              <span className="font-semibold text-white">Live Deployed URL:</span> Show the live Cloud Run application URL running in your browser.
            </li>
          </ol>
        </div>
      </div>

      {/* LinkedIn Post Copy Generator */}
      <div className="bg-slate-900 border border-slate-800 rounded-2xl p-6 space-y-4">
        <div className="flex items-center justify-between">
          <div className="flex items-center gap-2">
            <Linkedin className="w-5 h-5 text-blue-400" />
            <h3 className="text-base font-bold text-white">LinkedIn Submission Post Template (Mandatory)</h3>
          </div>
          <button
            onClick={handleCopyPost}
            className="flex items-center gap-1.5 text-xs font-semibold bg-blue-600 hover:bg-blue-500 text-white px-4 py-2 rounded-xl transition-all shadow cursor-pointer"
          >
            {copiedPost ? <Check className="w-4 h-4" /> : <Copy className="w-4 h-4" />}
            <span>{copiedPost ? 'Copied to Clipboard!' : 'Copy Post Content'}</span>
          </button>
        </div>

        <p className="text-xs text-slate-400">
          Tag <span className="text-blue-400 font-semibold">@InfoBharatInterns</span> and attach your screen recording video to fulfill the submission requirement.
        </p>

        <div className="bg-slate-950 border border-slate-800 rounded-xl p-4 font-mono text-xs text-slate-300 whitespace-pre-wrap leading-relaxed">
          {linkedInText}
        </div>
      </div>
    </div>
