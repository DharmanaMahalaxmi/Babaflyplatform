import os
from pydantic_settings import BaseSettings

class Settings(BaseSettings):
    PROJECT_NAME: str = "BabaFly Platform Backend API"
    VERSION: str = "1.0.0"
    API_PREFIX: str = "/api"
    SECRET_KEY: str = os.getenv("SECRET_KEY", "babafly-super-secure-secret-key-2026-internship")
    ALGORITHM: str = "HS256"
    ACCESS_TOKEN_EXPIRE_MINUTES: int = 60 * 24  # 24 hours
    
    # Database: Supports PostgreSQL or fallback to SQLite for local development & automated testing
    DATABASE_URL: str = os.getenv("DATABASE_URL", "sqlite:///./babafly.db")

    class Config:
        env_file = ".env"
        case_sensitive = True

settings = Settings()
